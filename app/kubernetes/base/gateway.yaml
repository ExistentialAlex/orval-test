---
apiVersion: launchpad.sky.uk/v1alpha1
kind: Gateway
metadata:
  name: orval-test-app
spec:
  realms:
    - name: ping
      auth: none
      allowInternet: false
      allowOnPrem: true
      routes:
        - path: / # https://orval-test-app.internal.test.adtechplatform.sky.com/
          methods:
            - GET

  services:
    - name: orval-test-app
      port: 80
